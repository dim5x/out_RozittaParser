# ── Основные зависимости ──────────────────────────────────────────────────────
PySide6>=6.6.0
Telethon>=1.35.0
python-docx>=1.1.0
python-dotenv>=1.0.0

# ── Ускорение шифрования Telethon ─────────────────────────────────────────────
# cryptg даёт 10-20x прирост скорости MTProto — рекомендуется
cryptg
pyaes

# ── Прокси (опционально) ──────────────────────────────────────────────────────
# Раскомментируйте если используете SOCKS5/MTProto прокси:
python-socks

# ── Расширенные фильтры сообщений (опционально) ───────────────────────────────
# Раскомментируйте для поддержки expression-фильтров (P2-2 в roadmap):
simpleeval

# ── Распознавание речи STT (опционально, тяжёлая зависимость ~500 МБ) ─────────
# Раскомментируйте для расшифровки голосовых и кружочков:
faster-whisper

# ── Импорт сессии из Telegram Desktop (опционально) ───────────────────────────
# Раскомментируйте для кнопки «Импорт из Telegram Desktop»:
opentele

# ── Сборка .exe (только для разработчиков) ────────────────────────────────────
pyinstaller>=6.0.0
